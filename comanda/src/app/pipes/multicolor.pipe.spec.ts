import { MulticolorPipe } from './multicolor.pipe';

describe('MulticolorPipe', () => {
  it('create an instance', () => {
    const pipe = new MulticolorPipe();
    expect(pipe).toBeTruthy();
  });
});
