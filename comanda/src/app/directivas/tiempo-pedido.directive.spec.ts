import { TiempoPedidoDirective } from './tiempo-pedido.directive';

describe('TiempoPedidoDirective', () => {
  it('should create an instance', () => {
    const directive = new TiempoPedidoDirective();
    expect(directive).toBeTruthy();
  });
});
