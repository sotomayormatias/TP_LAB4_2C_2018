import { TooltipDemoraDirective } from './tooltip-demora.directive';

describe('TooltipDemoraDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipDemoraDirective();
    expect(directive).toBeTruthy();
  });
});
