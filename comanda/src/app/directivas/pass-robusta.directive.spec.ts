import { PassRobustaDirective } from './pass-robusta.directive';

describe('PassRobustaDirective', () => {
  it('should create an instance', () => {
    const directive = new PassRobustaDirective();
    expect(directive).toBeTruthy();
  });
});
